name: Sync PR Diff to Port (Simple)

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sync-pr-diff:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Process PR and update Port
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PORT_CLIENT_ID: ${{ secrets.PORT_CLIENT_ID }}
          PORT_CLIENT_SECRET: ${{ secrets.PORT_CLIENT_SECRET }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_ID: ${{ github.event.pull_request.id }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_HTML_URL: ${{ github.event.pull_request.html_url }}
          PR_ADDITIONS: ${{ github.event.pull_request.additions }}
          PR_DELETIONS: ${{ github.event.pull_request.deletions }}
          PR_CHANGED_FILES: ${{ github.event.pull_request.changed_files }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          python3 << 'EOF'
          import os
          import requests
          import json
          import subprocess
          
          def get_pr_files():
              """Get list of changed files using gh CLI"""
              try:
                  result = subprocess.run(
                      ['gh', 'pr', 'diff', os.environ['PR_NUMBER'], '--name-only'],
                      capture_output=True,
                      text=True,
                      check=True
                  )
                  files = [f.strip() for f in result.stdout.split('\n') if f.strip()]
                  return files
              except subprocess.CalledProcessError:
                  return []
          
          def get_pr_diff():
              """Get PR diff using gh CLI"""
              try:
                  result = subprocess.run(
                      ['gh', 'pr', 'diff', os.environ['PR_NUMBER']],
                      capture_output=True,
                      text=True,
                      check=True
                  )
                  # Truncate if too long
                  diff = result.stdout
                  if len(diff) > 8000:
                      diff = diff[:8000] + "\n\n... (diff truncated)"
                  return diff
              except subprocess.CalledProcessError:
                  return "Error retrieving diff"
          
          def create_diff_content():
              """Create formatted markdown content"""
              files = get_pr_files()
              diff = get_pr_diff()
              
              content = f"""## 📊 Pull Request: {os.environ['PR_TITLE']}
          
          ### 📈 Change Statistics
          - **Files Changed:** {os.environ['PR_CHANGED_FILES']}
          - **Lines Added:** +{os.environ['PR_ADDITIONS']}
          - **Lines Deleted:** -{os.environ['PR_DELETIONS']}
          
          ### 📁 Modified Files
          """
              
              for file in files[:20]:  # Limit to first 20 files
                  content += f"- `{file}`\n"
              
              if len(files) > 20:
                  content += f"- ... and {len(files) - 20} more files\n"
              
              content += f"""
          ### 🔍 Code Changes
          
          ```diff
          {diff}
          ```
          
          [📖 View Full Diff on GitHub]({os.environ['PR_HTML_URL']}/files)
          """
              
              return content, files
          
          def get_port_token():
              """Get Port access token"""
              response = requests.post(
                  'https://api.getport.io/v1/auth/access_token',
                  json={
                      'client_id': os.environ['PORT_CLIENT_ID'],
                      'client_secret': os.environ['PORT_CLIENT_SECRET']
                  }
              )
              response.raise_for_status()
              return response.json()['accessToken']
          
          def update_port_entity():
              """Update Port entity with diff content"""
              diff_content, files = create_diff_content()
              token = get_port_token()
              
              url = f"https://api.getport.io/v1/blueprints/githubPullRequest/entities/{os.environ['PR_ID']}"
              
              payload = {
                  'properties': {
                      'diffContent': diff_content,
                      'additions': int(os.environ['PR_ADDITIONS']),
                      'deletions': int(os.environ['PR_DELETIONS']),
                      'changedFiles': int(os.environ['PR_CHANGED_FILES']),
                      'filesChanged': files
                  }
              }
              
              headers = {
                  'Authorization': f'Bearer {token}',
                  'Content-Type': 'application/json'
              }
              
              response = requests.patch(url, json=payload, headers=headers)
              
              if response.status_code in [200, 201]:
                  print(f"✅ Successfully updated Port entity {os.environ['PR_ID']}")
              else:
                  print(f"❌ Failed to update Port entity: {response.status_code}")
                  print(f"Response: {response.text}")
                  exit(1)
          
          # Main execution
          if __name__ == "__main__":
              try:
                  update_port_entity()
              except Exception as e:
                  print(f"❌ Error: {str(e)}")
                  exit(1)
          EOF
